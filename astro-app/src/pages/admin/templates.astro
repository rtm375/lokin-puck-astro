---
import Layout from "@layouts/dashboard.astro";
export const prerender = false;
const { t } = Astro.locals;
---

<Layout title={t("navigations.defaults.templates")}>
  <div class="h2">{t("navigations.defaults.templates")}</div>
</Layout>
