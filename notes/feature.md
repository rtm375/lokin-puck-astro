# Supabase migration task

Create a supabase migration task for the website table

In the website there will be

domain (lokin.cloud or lokin.id or lokin.com) no https just domain
name (website name)
description (website description)
pages (i am not sure do i need separate table for this?)
user uid (this website attached to which user)
settings (json data to store website settings)

so after user click create website, user can go to page builder puck editor there they can create a page e.g: home, about etc... 
we need to work what best for the page
since it will storing puck editor data like this:
https://puckeditor.com/docs/api-reference/data-model/data