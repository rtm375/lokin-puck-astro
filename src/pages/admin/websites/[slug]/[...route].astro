---
import StoreResetListener from "@/components/client/StoreResetListener";
import { Websites } from "@components/client/website/Websites";
const { preferences, user } = Astro.locals;
const { title } = Astro.props;
export const prerender = false;
---

<!doctype html>
<html lang={preferences?.language}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <Websites user={user} client:only="react" />
    <StoreResetListener client:only="react" />
  </body>
</html>
